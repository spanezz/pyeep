session_name: pyeep-heartbeat
windows:
  - window_name: Main
    layout: even-vertical
    shell_command_before:
      - cd ~/dev/pyeep/
    panes:
      - python3 -m pyeep.hub -v --config scenes/heartbeat-cfg.yaml
      - python3 -m pyeep.app.inspector --hub .webtoken -v
      # CYCPLUS H1
      # - python3 -m pyeep.heartrate --hub .webtoken -v --addr F9:EC:3D:7A:10:C2
      # Moofit
      # - python3 -m pyeep.heartrate --hub .webtoken -v --addr CD:E3:36:F6:BB:74
      # Mock
      - python3 -m pyeep.heartrate --hub .webtoken -v
      # - python3 -m pyeep.happylights --hub .webtoken -v -a 21:04:99:10:35:05
      - python3 -m pyeep.happylights --hub .webtoken -v
